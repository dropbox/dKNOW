[package]
name = "docling-parse-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors = ["dropbox <ayates@dropbox.com>"]
description = "Raw FFI bindings to docling-parse C API (workspace-only, not published)"
keywords = ["ffi", "bindings", "c-api", "raw"]
categories = ["external-ffi-bindings"]
publish = false
[build-dependencies]
bindgen = "0.69"

[dependencies]
# No dependencies - just raw FFI
